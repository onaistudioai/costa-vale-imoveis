node_modules/

# Todo arquivo de ambiente fica fora do repositorio. O padrao amplo e de
# proposito: fixar `.env` e `.env.local` deixava passar um `.env.production`
# criado por engano, que e justamente o que tem segredo de verdade.
.env*
!.env.example
.vitest/
.next/
.next/
next-env.d.ts

# Artefato de build do TypeScript — regerado a cada `tsc`.
tsconfig.tsbuildinfo

# Material de referência de terceiros (PDF do guia de agentes). Fica na pasta
# de trabalho, mas não no repositório: é material de outra pessoa e pesa 1 MB.
guia regra pra criação de agente/

.vercel
.env*
